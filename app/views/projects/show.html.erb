<button class="btn btn-danger"><%= link_to "Delete Project", project_path(@project),
                        :data => {:confirm => "Are you sure?",
                                       :method => "delete"} %></p>
