<h1>Projects</h1>

  <ul>
    <% @projects.each do |project| %>
      <li><%=link_to project.title, project_path(project)%></li>
    <% end %>
  </ul>

 <button class="btn btn-default"><%= link_to "New Project", new_project_path %></button>
